#!/bin/sh
certbot certonly --agree-tos --preferred-challenges dns-01 \
    --manual
    --manual-auth-hook /root/bin/certbot-{{provider}}-dns-auth \
    --manual-cleanup-hook /root/bin/certbot-{{provider}}-dns-cleanup \
    --email $1 \
    --cert-name $2 -d $2 -d *.$2
