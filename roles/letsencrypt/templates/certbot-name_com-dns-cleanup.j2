#!/bin/sh

AUTH="{{dns[dns_provider + '_api_username']}}:{{dns[dns_provider + '_api_secret']}}"
HOST="{{dns[dns_provider + '_api_host']}}"

exec curl $HOST/v4/domains/$CERTBOT_DOMAIN/records/$CERTBOT_AUTH_OUTPUT \
    -u "$AUTH" \
    -X DELETE
